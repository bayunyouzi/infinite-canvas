- 修复视频生成功能中 OpenAI 兼容 `/videos` 接口上传参考图失败的问题：`web/src/services/api/video.ts` 的 `createOpenAIVideoTask` 不再对 `FormData` 请求强制写入 `Content-Type: application/json`，避免 `grok-imagine-video-1.5-720p` 的 `input_reference[]` 被服务端按 JSON 错误解析。
- 完成针对性自测：用本地 Node 脚本对比了同一 `FormData` 在自动请求头与强制 `application/json` 下的实际发送结果，确认修复后浏览器/运行时会按 `multipart/form-data; boundary=...` 正常提交参考图。
- 更新待测试文档，补充本次视频参考图提交修复项，方便后续人工联调验证。
- 已将本次修复同步推送到 GitHub 仓库 `bayunyouzi/infinite-canvas` 的 `main` 分支，提交为 `a98db5dca0cd0b576811f9e7f0439438039eee6a`，提交信息为 `fix: submit video references as multipart form`。
- 用用户提供的 `https://ai.bayunzi.shop` 与指定 API Key 对 `grok-imagine-video-1.5-720p` 做了真实联调：`/v1/models` 可正常返回模型，说明密钥有效；但 `/v1/videos` 返回 `404 page not found`，`/videos` 返回站点 HTML 页面，说明当前 Base URL 不是项目所需的视频 API 网关地址。
- 在 `web/src/services/api/video.ts` 增加了视频接口文本/HTML 错误识别：当渠道返回 404 文本页或整站 HTML 时，前端会明确提示“未开放视频接口”或“当前 Base URL 是站点页面不是视频 API”，避免再次出现模糊报错。
- 根据用户补充的 Grok 视频 1.5 文档，已把 `grok-imagine-video-1.5(-720p/-1080p)` 改为专用异步 JSON 链路：创建任务走 `POST /v1/videos/generations`，请求体使用 `image: { url }`、`duration`、`aspect_ratio`，创建后读取 `request_id`，轮询走 `GET /v1/videos/{request_id}`，完成时优先取返回中的 `video.url`。
- 用用户提供的 `https://ai.bayunzi.shop` 再次按新文档做真实探测：`POST /v1/videos/generations` 依然返回 `404 page not found`，说明当前阻塞点已确认是渠道网关未开放 Grok 1.5 视频端点，而不是项目内请求格式错误。
- 已检查用户的 sub2api 项目 `D:/其他/xiangmu/zizhisub-main`：后端为 Go + Gin + Ent，`backend/internal/server/routes/gateway.go` 仅注册了 `/v1/models`、`/v1/messages`、`/v1/responses`、`/v1/chat/completions`、`/v1/embeddings`、`/v1/images/*`，没有任何 `/v1/videos/generations` 或 `/v1/videos/{id}` 路由；handler 层也没有 Videos 实现，因此 `ai.bayunzi.shop` 的 404 根因已坐实为 sub2api 后端当前版本未实现视频网关。
